## Cleanup Plan for Forge & Fable Repo

### What stays untouched
- `docs/`, `QA/`, `rules-research/` — kept as-is
- All core project files (`src/`, `public/`, `tests/`, `scripts/`, configs, etc.)
- All untracked working-tree files (the new AO-17 campaign workspace feature work)

### Step 1: Update `.gitignore`
Add entries to prevent re-accumulation:
```
# Backup directories
.portrait-module-backup/
.codex-vault-test/

# Temp/scratch
/tmp/
```

### Step 2: Remove root-level loose junk (14 files)
- `backdrop.png` — duplicate of `public/backdrop.png` (8.7 MB)
- `initiativetracker.gif`, `rolltray.gif` — demo GIFs
- `round1_paper.py`, `round3_fix.py`, `round3_stage1.py` — ad-hoc scripts
- `wcl_*.json` (5 files) — World of Warcraft combat logs, completely unrelated
- `GITHUB_SETUP.md` — one-time setup note
- `dev-server.err.log`, `dev-server.log` — already gitignored, just delete locally

### Step 3: Remove junk directories (5 directories)
- `misc/` — stale duplicate of root configs (~11 MB)
- `assets/` — design working files, raw portraits, AI concept art (~40+ MB)
- `mockups/` — UI mockup HTMLs and screenshots (~14 MB)
- `.portrait-module-backup/` — pre-migration backup (local cleanup)
- `.codex-vault-test/` — test SQLite database (local cleanup)

### Step 4: Remove Next.js boilerplate SVGs from `public/`
- `file.svg`, `globe.svg`, `next.svg`, `vercel.svg`, `window.svg`

### Step 5: Single commit
`chore: repo cleanup — remove ~84MB of cruft, design artifacts, and unrelated files`

**Nothing else is touched.** All source code, tests, docs, QA, and rules-research stay exactly as-is.