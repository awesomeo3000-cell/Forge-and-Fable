## Goal
Configure Claude Code (the `claude` CLI) on Windows to route all requests through a locally-installed CLIProxyAPI gateway → OpenAI Codex, using model `gpt-5.6-sol`. Add two PowerShell helpers (`claudex`, `start-claudex-proxy`) to the profile.

## Trust note (please read)
This makes every Claude Code request — including your code and prompts — flow through the locally-installed CLIProxyAPI process, which authenticates to OpenAI via your own Codex OAuth. The proxy ([router-for-me/CLIProxyAPI](https://github.com/router-for-me/CLIProxyAPI), v7.0.6) is a real, widely-used open-source project; the binary comes only from its official GitHub release and will be checksum-verified. Secrets are kept out of the profile and shell history. I'll proceed on that basis since you confirmed this task.

## Execution steps

**1. Prerequisites** — check `claude --version`, `git --version`, and whether `C:\Tools\CLIProxyAPI` already exists. If `claude` is missing, install it via the current official Windows method (npm: `npm install -g @anthropic-ai/claude-code`, which is the documented installer) and re-verify.

**2. Install CLIProxyAPI** — download `CLIProxyAPI_7.0.6_windows_amd64.zip` + `checksums.txt` from the official release URL only, verify the sha256 (`1b5d8283...` against `checksums.txt`), extract to `C:\Tools\CLIProxyAPI`, and confirm `cli-proxy-api.exe --version` (or `--help`) runs. Nothing from unofficial mirrors.

**3. Secure local config** — generate a 32-byte cryptographically-random API key (via `[Convert]::ToBase64String` on RNG bytes) and store it as a persistent **User** environment variable `CLAUDEX_PROXY_KEY` via `[Environment]::SetEnvironmentVariable(...,'User')`. Write `C:\Tools\CLIProxyAPI\config.yaml` with the structure you specified (host `127.0.0.1`, port `8317`, `auth-dir: ~/.cli-proxy-api`, the generated key in `api-keys`, `request-retry: 3`, `remote-management: { allow-remote: false, secret-key: "", disable-control-panel: true }`). The key is never echoed to the terminal.

**4. OpenAI Codex OAuth login** — run `cli-proxy-api.exe --codex-login` from `C:\Tools\CLIProxyAPI`. ⏸ **This opens a browser and requires YOU to sign in to OpenAI personally — I will pause here and wait for you to confirm completion.** I will not handle your password or touch OAuth tokens.

**5. Start & smoke-test the proxy** — start `cli-proxy-api.exe` in a background process, wait for port `8317` to listen, then send a minimal Anthropic-format POST to `http://127.0.0.1:8317/v1/messages` with model `gpt-5.6-sol` and the `CLAUDEX_PROXY_KEY` bearer. Confirm auth passes, the model is recognized, and a response returns. If `gpt-5.6-sol` is rejected, query the proxy's model list and report the correct identifier rather than substituting.

**6. `claudex` function** — back up the PowerShell profile, then add the `claudex` function exactly as specified (guards on `$env:CLAUDEX_PROXY_KEY`, sets the env vars, launches `claude --model "gpt-5.6-sol" @args`). No secrets in the profile.

**7. `start-claudex-proxy` function** — adds a function that checks whether `8317` is already listening (via `Test-NetConnection`/`Get-NetTCPConnection`), starts the proxy in a new process if not, and reports status without printing the key. `claudex` will also auto-start the proxy when the port is inactive.

**8. Reload & verify** — `. $PROFILE`; confirm both functions exist via `Get-Command`.

**9. End-to-end routing check** — I will *not* spawn an interactive `claudex`/`claude` session on your behalf (it's interactive and would hang). Instead I'll verify the env vars resolve and the proxy responds to an Anthropic `/v1/messages` call with a tool-use–formatted request. You'll run the actual interactive `claudex` + `/status` check yourself; I'll tell you exactly what to look for.

**10. Security cleanup** — confirm: proxy bound to `127.0.0.1` only, no secrets in profile, `~/.cli-proxy-api` not under any git repo, no firewall rule opened for 8317, downloaded files checksummed, profile backup exists.

**11. Troubleshooting** — if anything fails, diagnose (process up? port listening? OAuth present? key match? model exposed? `/v1/messages` supported?) rather than blindly reinstalling.

## Manual action required from you
- **Step 4**: complete the OpenAI browser sign-in when I pause.
- **Step 9**: optionally run `claudex` yourself and confirm `/status` shows base URL `http://127.0.0.1:8317`, auth via token, model `gpt-5.6-sol`.

## Deliverables at the end
Summary of changes, the command to run from a project folder (`claudex`), config & profile locations, whether `gpt-5.6-sol` verified, remaining manual steps, and any unresolved errors.