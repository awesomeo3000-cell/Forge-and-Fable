## Plan: Dreamwright & Classic Style Tabs for Chapter II "The Likeness"

### Current State
- Chapter II "The Likeness" already exists at step index 1 in the creator wizard
- 40 portraits in `public/portraits/drop-ins/` (3 batches: 10 UUID, 8 `u1828259955`, 22 `u3847362411`)
- `PortraitSelectorModal` has "Suggested" / "All Portraits" tabs
- `PortraitOption` type has no `style` field

### Implementation

#### 1. Add `style` field to the portrait data model

**`src/data/portraits.ts`** — Add to `PortraitOption`:
```ts
style: "dreamwright" | "classic";
```
Also add a helper: `PORTRAITS_BY_STYLE` map for O(1) style filtering.

**`src/data/portraits.generated.ts`** — Add `style` to all 40 entries. Based on the filename batches:
- `u3847362411` prefix (22 files) → `"dreamwright"` (newer, blue arcane design)
- `u1828259955` prefix (8 files) → `"classic"` (older, darker, serious tone)
- UUID-only (10 files) → assign based on visual inspection or default to `"classic"`

#### 2. Replace Suggested/All tabs with Dreamwright/Classic/All

**`src/components/portraits/PortraitSelectorModal.tsx`:**
- Change `TabId` type from `"suggested" | "all"` to `"dreamwright" | "classic" | "all"`
- Replace the two tab buttons with three: **Dreamwright** | **Classic** | **All Portraits**
- Tab labels: simple, no ancestry metadata exposed
- Filtering: `displayPortraits = PORTRAITS.filter(p => tab === "all" || p.style === tab)`
- Default tab: `"dreamwright"` (show the newer style first)
- Maintain existing "Suggested" ancestry logic as a sort preference within each tab (suggested portraits appear first in the grid, never filtered out)

#### 3. Update CreatorPanel Step 1 inline portrait section

**`src/components/CreatorPanel.tsx`** (lines ~423-507):
The step already renders a portrait section. Ensure it also reflects the style tabs if showing an inline gallery, or simply opens the updated modal with the new tabs.

#### 4. Update CSS

**`src/app/globals.css`:**
- Three tabs instead of two — minor width adjustments
- Dreamwright tab gets a subtle blue-arcane accent (cool indigo/blue tone matching the style)
- Classic tab gets a warm amber/parchment accent (matching the darker old-school tone)
- No visible labels or metadata exposed

#### 5. Update sync script (if applicable)

**`scripts/sync-portraits.py`** — Ensure the generated `portraits.generated.ts` includes the `style` field when new drop-ins are synced.

### Files Changed

| File | Action |
|---|---|
| `src/data/portraits.ts` | Add `style` field to `PortraitOption`, add `PORTRAITS_BY_STYLE` helper |
| `src/data/portraits.generated.ts` | Add `style` to all 40 entries |
| `src/components/portraits/PortraitSelectorModal.tsx` | Replace Suggested/All with Dreamwright/Classic/All tabs |
| `src/components/CreatorPanel.tsx` | Minor: ensure inline portrait section respects style tabs (if needed) |
| `src/app/globals.css` | Tab styling for 3 tabs, accent colors |
| `scripts/sync-portraits.py` | Add style field to generated output (if applicable) |

### What stays the same
- Chapter II "The Likeness" step position, chapter metadata, stepComplete logic
- CharacterPortrait renderer (no changes needed)
- PortraitField component (no changes needed)
- `portraitUrl` persistence (no changes)
- All existing accessibility patterns (focus trap, aria roles, keyboard nav)
- No player-facing labels or ancestry metadata
