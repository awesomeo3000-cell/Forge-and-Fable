import { NextResponse } from "next/server";import { authenticateRequest } from "@/lib/auth";import { shareHandout } from "@/lib/dmToolsStore";import { dmToolsError } from "@/lib/dmToolsRoute";
export const runtime="nodejs";type C={params:Promise<{id:string;handoutId:string}>};export async function POST(request:Request,{params}:C){try{const userId=await authenticateRequest(request),{id,handoutId}=await params;const body=await request.json().catch(()=>({})) as {recipientUserId?:unknown};const recipientUserId=typeof body.recipientUserId==="string"&&body.recipientUserId.trim()?body.recipientUserId.trim():null;return NextResponse.json({handout:shareHandout(id,userId,handoutId,recipientUserId)});}catch(error){return dmToolsError(error,"Could not share handout.");}}
