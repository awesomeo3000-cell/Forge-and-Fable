import { NextResponse } from "next/server";import { authenticateRequest } from "@/lib/auth";import { endSession } from "@/lib/dmToolsStore";import { dmToolsError } from "@/lib/dmToolsRoute";
export const runtime="nodejs";type C={params:Promise<{id:string;sessionId:string}>};export async function PATCH(request:Request,{params}:C){try{const userId=await authenticateRequest(request),{id,sessionId}=await params,body=await request.json() as {action?:string};if(body.action!=="end")throw new Error("Unsupported session action.");return NextResponse.json({session:endSession(id,userId,sessionId)});}catch(error){return dmToolsError(error,"Could not update session.");}}
