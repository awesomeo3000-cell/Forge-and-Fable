import { NextResponse } from "next/server";import { authenticateRequest } from "@/lib/auth";import { respondLoot } from "@/lib/dmTable/worldStore";import { dmToolsError } from "@/lib/dmToolsRoute";
export const runtime="nodejs";export async function POST(request:Request,{params}:{params:Promise<{id:string;parcelId:string}>}){try{const userId=await authenticateRequest(request),{id,parcelId}=await params,body=await request.json() as {itemId?:string;accept?:boolean};if(!body.itemId||typeof body.accept!=="boolean")throw new Error("Item and response are required.");return NextResponse.json(respondLoot(id,userId,parcelId,body.itemId,body.accept));}catch(error){return dmToolsError(error,"Could not respond to loot.");}}
