import { NextResponse } from "next/server";import { authenticateRequest } from "@/lib/auth";import { updateJournalEntry } from "@/lib/dmToolsStore";import { dmToolsError } from "@/lib/dmToolsRoute";
export const runtime="nodejs";type C={params:Promise<{id:string;entryId:string}>};export async function PATCH(request:Request,{params}:C){try{const userId=await authenticateRequest(request),{id,entryId}=await params;return NextResponse.json({entry:updateJournalEntry(id,userId,entryId,await request.json())});}catch(error){return dmToolsError(error,"Could not update journal entry.");}}
