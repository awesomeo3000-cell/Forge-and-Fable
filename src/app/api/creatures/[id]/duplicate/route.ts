import { NextResponse } from "next/server";import { authenticateRequest } from "@/lib/auth";import { duplicateCreature } from "@/lib/dmToolsStore";import { dmToolsError } from "@/lib/dmToolsRoute";
export const runtime="nodejs";type C={params:Promise<{id:string}>};export async function POST(request:Request,{params}:C){try{const userId=await authenticateRequest(request),{id}=await params,body=await request.json().catch(()=>({})) as {campaignId?:string};return NextResponse.json({creature:duplicateCreature(userId,id,body.campaignId)},{status:201});}catch(error){return dmToolsError(error,"Could not duplicate creature.");}}
