import { NextResponse } from "next/server";import { authenticateRequest } from "@/lib/auth";import { generateCampaignEncounter } from "@/lib/dmToolsStore";import { dmToolsError } from "@/lib/dmToolsRoute";import type { GenerateEncounterInput } from "@/lib/encounterGenerator";
export const runtime="nodejs";export async function POST(request:Request){try{const userId=await authenticateRequest(request),input=await request.json() as GenerateEncounterInput;if(!input.campaignId||!input.seed||!["easy","medium","hard","deadly"].includes(input.difficulty))throw new Error("Campaign, seed, and difficulty are required.");return NextResponse.json({encounter:generateCampaignEncounter(userId,input)});}catch(error){return dmToolsError(error,"Could not generate encounter.");}}
