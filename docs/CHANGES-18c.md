# CHANGES-18c — The commission (builder proper)

**Round:** R18 "The Ledger", sub-round c (proposal: `ai-project-proposal-18.md` §3/18c)
**Files touched:** `src/components/CreatorPanel.tsx`, `src/lib/ledgerCopy.ts` (created in 18a, consumed here), `src/app/globals.css` (append-only, `/* ── 18c ── */` under the R18 banner)

## Pass 1 — chrome

- Container gained `ledger-spread` beside `creator-panel paper-surface dj-dossier`. The key move: inside `.paper-surface.ledger-spread` the dossier's paper tokens (`--paper`, `--paper-raised`, `--ink/-2/-3`, `--doc-rule`, `--doc-accent`, …) are **re-pointed at the ledger tokens**, so every legacy `dj-*` rule in the spread inherits the ledger material without editing a single existing block. No token is defined in terms of itself (landmine list checked).
- Grid is `210px minmax(0,1fr)`, stacking to one column below 720px.
- `dj-rail` gained `ledger-toc`: `--ledger-tint` background, `--ledger-edge` right hairline, heading `THE COMMISSION` (11px letterspaced caps). Steps render `I. Provenance` … `VI. The Seal` from `CHAPTERS` (same array order/length as `steps` — only relabeled). The ✓●○ marker glyphs are **deleted**. Completed chapters show their decided value as right-aligned lowercase-italic marginalia with a trailing ✓ (name / class / background / species display-name / stat-method label), sourced from the same data `stepComplete` reads. Active chapter = `--ledger-seal` text + 2px seal left rule. `aria-current="step"` and click-to-jump untouched. Under the TOC: *Each chapter inks the record as it is decided.* The old rail label showed the build mode ("Standard"); it is replaced by the commission heading per spec.

## Pass 2 — the document page

- `dj-document-header` restyled by CSS only: title 20px, the StepSlot slash-line 12px italic `--ledger-faded`; markup untouched.
- Each step's section header is now `Chapter {numeral} · {name}` (20px Newsreader) with the §2 subtitle in italic beneath. The heading keeps `id="dj-section-title"`, so the section's `aria-labelledby` still resolves.
- Footer buttons became `ledger-button` (back = outline, `Previous chapter`) and `ledger-button-primary` (ink-filled) with the §2 chapter labels: `Record the name` → `Seal the vocation` → `Record the origin` → `Seal the lineage` → `Fix the attributes` → `Press the seal`. "Continue", "Forge Hero", the `ChevronRight` arrow, and the `Save` icon are gone. Disabled/validation logic (including the missing-fields `forgeError`) is byte-identical.

## Pass 3 — option rows

- **Classes (Ch. II):** the `dj-card-grid` of icon cards became `.ledger-option.has-dot` ruled rows: 10px seal dot (`--class-a` via `data-class`, mixed 75% toward ink per the paper-color landmine), small-caps class name, italic descriptor from `CLASS_DESCRIPTORS` verbatim, `Preview class` as a seal-colored underlined text link (kept `aria-haspopup="dialog"`), selected = tint + 3px seal left rule + `SEALED ✦`. The invisible full-row `dj-card-select` overlay button (with `aria-label`/`aria-pressed`) is retained unchanged, as is the class-switch draft reset (skills/tools/HP rolls cleared).
- **Origins (Ch. III):** same rows with the neutral `--ledger-faded` dot; descriptors are the existing per-background lines reworded into the ledger register (*a criminal starting story* / *a personal story, written at the table*). Selection handler and `aria-pressed` unchanged.
- **Species (Ch. IV):** rows with neutral dots; descriptor = first sentence of the race `summary` already shipped in the ruleset, truncated ~60 chars (`firstSentence()`); full text remains in the Learn modals, which row click still opens (`aria-haspopup` kept). Family rows open the family picker and show `{n} subspecies` as quiet marginalia (or `SEALED ✦` once a member is chosen). The `ChevronRight` family chevron is deleted; the ⟶ affordance lives in row hover.
- **DossierStamp** kept but re-dressed as the wax seal: 44px `--class-a` circle holding the existing class/species SVG icon in paper color (this is where `ClassIconPlaceholder` continues to live), small-caps name, italic detail, `SEALED ✦` in seal color with no chip border. Origin stamps keep their `OR` seal on a `--ledger-seal` circle. `classDetailLine` no longer mentions the subclass level — that fact moved to the §2 footnote idiom: `† Subclass is chosen at 3rd level; the ledger will prompt you.` (ordinal adapts via `ordinalLevel` for non-3rd-level subclasses), shown under the stamp in Chapter II. Existing `dj-skill-hint` helper lines are footnoted via CSS (`† ` prefix, `--ledger-ghost`, italic) — no new hint text added.
- **Chapters V & VI keep their controls** — mechanics untouched, re-skinned only: inputs/selects transparent with `--ledger-rule` bottom borders and seal-colored focus; method-row buttons are small-caps quiet buttons (active = tint + 2px seal bottom rule) with their `CircleGauge`/`ScrollText`/`Dices`/`Pencil` icons removed (§0.7); the Roll 4d6 and HP-roll buttons became small ledger buttons, icons removed; the mini-stepper's `Minus`/`Plus` lucide glyphs became text `−`/`+` **with added `aria-label`s** (they previously had no accessible name — strict reading of §0.7 required removing the icons, and text-only −/+ needed labels to not regress a11y); the points pill is a flat small-caps counter; `Gem` deleted from the finalize panel; final-loadout pills flattened to italic hairline entries.
- Zero lucide imports remain in `CreatorPanel.tsx`.

## Verification — what was actually done

- `npx tsc --noEmit` clean. `npm run lint`: 0 errors (one pre-existing unrelated warning). **`npm run build` clean** (Next 16.2.9/Turbopack, all routes compile).
- Dev server hot-compiled; one transient `ChevronRight is not defined` overlay appeared *between* two edits mid-refactor (imports removed before the footer JSX was updated) and resolved with the completing edit — current source contains no reference.
- Logic diff audit: every `onDraftChange`/`onStepChange`/`onMethodChange` call site, the draft-reset semantics on class/background change, `canContinue`, `stepComplete`, and the Learn/family modal wiring are unchanged — the refactor moved markup and classNames only.
- **Honest gap:** the specced start-to-finish browser run (rogue/halfling/point-buy creation, TOC jumping, keyboard-only chapters I–II, chapter screenshots) could not be executed by the implementer — the permission classifier blocks reaching the local dev server. `scripts/r18-seed.mjs` is ready to seed the reviewer account; the checklist in §3/18c remains open for the human/next reviewer pass.
